# Required-session provider probe

Next offline preparation while AccelPix access remains pending, 6 September 2026.

The EOD schema probe can currently succeed when the response contains only
neighboring sessions. Extend the existing CLI with repeatable
`--required-session YYYY-MM-DD` arguments. Reject dates outside the request
before loading credentials or making requests. Missing requested sessions
must produce a nonzero result and list the missing dates. Report observed
dates without printing prices or tokens. Preserve existing schema-only use
when no required sessions are specified. Session presence must not certify
adjustment factors or data admissibility.

Prepare bounded commands for the four missing sessions and the four February
factor discrepancies. Make no authenticated requests while access is pending.
This prepares data acceptance; it does not evaluate or promote a strategy.
