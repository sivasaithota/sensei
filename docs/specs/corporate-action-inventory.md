# Inventory of observed scaling and primary action evidence

Pass 8 classifies the finite set of symbols whose saved held-trade endpoints
show Kite/raw volume scaling differing from one by more than 1%. Reuse the
complete held-price screen, retaining all trade/end-point counts and missing
observations. Do not use these volume ratios to infer corporate-action terms.

Create a separately reviewed primary-evidence classification registry with
explicit split, bonus, split-and-bonus, demerger or unresolved categories. Bind
the cited local research-note hashes. A symbol's classification establishes
that the documented action exists; it does not prove the cumulative vendor
transformation or complete corporate-action coverage.

The inventory groups all source records by symbol, retaining matched, missing
and non-unit endpoint counts, exact observed ratio ranges, affected endpoint
date bounds, and exact trade record references. Require registry symbols to
match the flagged symbol set exactly so additions/omissions cannot silently
disappear. Missing source notes or changed screen/proof/note bytes must fail.

Compute global/per-class coverage counts. Leave unflagged symbols unclassified
rather than treating a ratio of one as evidence that no action occurred. Preserve
DATA_BLOCKED, research-only and can_trade=false. Do not change price data,
quantities, strategy, cash, fills or corporate-action entitlements.

Test grouping with duplicated trades and missing endpoints, absent/extra registry
entries, changed provenance and unknown classification. Generate the real 27-stock
inventory, independently review source classifications/results, document the
next accounting step, and commit/push after required checks.

Also apply the existing split-unit audit, without changing its rules, to the
eight newly evidenced pure splits. Keep this ledger separate from the original
MAZDOCK/HEG ledger and exclude the combined Bajaj Finance split/bonus. Retain
scaling mismatches and all unevaluated trades; do not imply complete-unit coverage.
