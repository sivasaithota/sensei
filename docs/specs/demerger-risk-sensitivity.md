# Demerger reconciliation and entry-risk sensitivity

Owner continuation, 7 September 2026: proceed with corporate-action work before
interpreting the first Kite baseline or changing the strategy. Primary sources
show Kite uses company tax cost allocations for these demergers. Such a chart
convention does not establish entitlement-inclusive portfolio returns.

1. Retain exact raw and Kite bars, published allocation percentages and source
   dates for ABFRL and VEDL. Compare observed ratios with those percentages;
   never apply another full allocation or force the ex-date return to zero.
2. Freeze a separate research sensitivity policy before evaluation. Preserve
   the same universe, prices, benchmark, strategy parameters, fees and risk
   settings. Only new-entry eligibility changes. The original baseline remains
   reproducible and is rerun as a control.
3. Block new entries in the two named stocks from the conservative availability
   date following the announcement until at least 252 post-event observations
   exist strictly before the entry session. No earlier decision may see a later
   announcement. Retain all stocks and sessions. This is an explicit sensitivity
   exclusion, not a claim that excluded bars have been repaired or certified.
4. Bind policy bytes and implementation to the frozen run identity. Validate
   unique event IDs, dates, symbols, source URLs and observation threshold. A
   missing configured policy is an error, never a fallback to unguarded trading.
5. Check every simulated holding crossing an unresolved ex-date, including an
   exit on that date. If any crosses, withhold the economic verdict and report
   the unmodeled entitlements explicitly. No invented cash/shares or automatic
   credit to an already adjusted series. Require end liquidation so this check
   sees all positions, and report the policy as partial coverage of two events.
6. Report the actual control/sensitivity comparison without optimizing the
   threshold or selecting a strategy based on the outcome. Both remain reused
   development history, data-blocked and unable to authorize trades. Explain
   what accounting and historical-membership evidence is still required.
